export type nameProps = {
    first: string,
    last: string
}

export type personProps = {
    name: nameProps
}

export type userProps = {
    name: string,
    email: string
}

//

export type selectedModuleProps = {
    id: 1,
    name: string,
    value: string,
    rangeMax: number,
    rangeMin: number,
    risk: number,
}

